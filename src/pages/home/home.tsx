import { Navbar } from '../../components/navbar';

interface HomeProps {}

const Home = (props: HomeProps) => {
	return (
		<section>
			<Navbar /> Home Page
		</section>
	);
};

export default Home;
